# STAC extensions required whenever pointcloud/projection fields (`proj:epsg`, `pc:type`) are present on a collection's summaries.

STAC extensions required whenever pointcloud/projection fields
(`proj:epsg`, `pc:type`) are present on a collection's summaries.

## Usage

``` r
required_lidar_stac_extensions()
```

## Value

Character vector of extension schema URLs
