# Extract spatial extent from VPC items

Extract spatial extent from VPC items

## Usage

``` r
extract_spatial_extent(vpc_obj)
```

## Arguments

- vpc_obj:

  VPC object

## Value

List with spatial extent structure
