% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_helpers.R
\name{update_footprints_asset}
\alias{update_footprints_asset}
\title{Recompute and register a collection's footprints GeoPackage asset}
\usage{
update_footprints_asset(collection_obj, collection_dir, vpc, crs)
}
\arguments{
\item{collection_obj}{Collection object (list); \verb{$assets} is updated}

\item{collection_dir}{Path to collection directory}

\item{vpc}{The combined VPC object, as returned by
\code{\link[=update_vpc_asset]{update_vpc_asset()}}}

\item{crs}{EPSG code to reproject the footprints to (the collection's
own CRS)}
}
\value{
Updated \code{collection_obj}
}
\description{
Reuses \code{\link[=write_gpkg]{write_gpkg()}} to convert the combined VPC (from
\code{\link[=update_vpc_asset]{update_vpc_asset()}}) into a queryable GeoPackage - one feature per
item, with its footprint geometry and metadata as attributes, unlike a
plain GeoJSON where per-tile attributes aren't easily queryable in GIS
software.
}
\details{
Deletes any existing file at the destination first: \code{\link[=write_gpkg]{write_gpkg()}}
checks its own \code{overwrite} argument before writing, but doesn't delete
the existing file itself, and \code{sf::st_write()} can otherwise behave
ambiguously (refuse, or append a duplicate layer) when the destination
GeoPackage already exists - which it will, since this gets called
again on the same path every time items are added.
}
\keyword{internal}
