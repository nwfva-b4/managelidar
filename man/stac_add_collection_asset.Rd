% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_functions.R
\name{stac_add_collection_asset}
\alias{stac_add_collection_asset}
\title{Add a visual asset (thumbnail, overview, ...) to a collection}
\usage{
stac_add_collection_asset(
  collection,
  source,
  roles = "thumbnail",
  key = NULL,
  title = NULL,
  copy = NULL
)
}
\arguments{
\item{collection}{Path to an existing \code{collection.json}.}

\item{source}{A URL, or a path to a local image file (png, jpg/jpeg,
webp, gif, or tif/tiff).}

\item{roles}{Character vector of asset roles. Defaults to
\code{"thumbnail"}. Multiple roles can be combined on the same image, e.g.
\code{c("thumbnail", "overview")}.}

\item{key}{Asset key (dictionary key under \code{assets}). Defaults to the
first role (e.g. \code{"thumbnail"}). A second call with the same key
replaces it.}

\item{title}{Asset title. Defaults to a title-cased version of \code{key}.}

\item{copy}{Whether to copy the image into the catalog tree rather than
reference it in place. Defaults to \code{FALSE} for URLs (already
web-accessible) and \code{TRUE} for local files (otherwise unreachable from
a browser via \code{\link[=stac_browse]{stac_browse()}}).}
}
\value{
Path to \code{collection.json} (invisibly), for piping into further
calls.
}
\description{
Registers an image as a collection-level asset, using \code{roles} to tell
STAC Browser (and other clients) how to display it - \code{"thumbnail"} is
shown as a preview image on the collection's own page. For an icon
shown in headers and listings, use \code{\link[=stac_set_icon]{stac_set_icon()}} instead - STAC
Browser treats icons as \emph{links}, not assets, which is a different
mechanism.
}
\examples{
cat <- tempfile("stac-managelidar-") |>
  stac_create_catalog(id = "lidar_ni") 
col <- cat |>
  stac_add_collection(id = "lidar_ni", title = "Lidar Solling", keywords = c("lidar", "ALS", "Solling", "test")) |> 
  stac_add_collection_asset(
    "https://raw.githubusercontent.com/nwfva-b4/managelidar/refs/heads/main/man/figures/logo.png",
    roles = "thumbnail"
  )

}
