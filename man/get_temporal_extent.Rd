% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_temporal_extent.R
\name{get_temporal_extent}
\alias{get_temporal_extent}
\title{Get the temporal extent of LASfiles}
\usage{
get_temporal_extent(
  path,
  per_file = TRUE,
  full.names = FALSE,
  from_csv = NULL,
  return_referenceyear = FALSE,
  fix_false_gpstime = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{path}{Character. Path to a LAS/LAZ/COPC file, a directory, or a Virtual
Point Cloud (.vpc) referencing these files.}

\item{per_file}{Logical. If \code{TRUE} (default), returns dates per file. If \code{FALSE},
returns combined date range of all files.}

\item{full.names}{Logical. If \code{TRUE}, filenames in the output are full paths;
otherwise base filenames (default). Only used when \code{per_file = TRUE}.}

\item{from_csv}{Character or NULL. If provided, path to a CSV file containing
acquisition dates for tiles without GPS time. The CSV must have columns
\code{minx}, \code{miny} (tile coordinates in km) and \code{date} (YYYY-MM-DD).
If NULL (default), dates for non-GPS tiles are extracted from file headers.}

\item{return_referenceyear}{Logical. If \code{TRUE}, returns the reference year instead
of the acquisition date (e.g., reference year 2015 for data acquired in
November or December 2014). Default is \code{FALSE}.}

\item{fix_false_gpstime}{Logical. If \code{TRUE} (default), detects files that claim
GPS time encoding in their header but contain week-second timestamps instead
of standard GPS time. Such files produce spurious dates in the range
2011-09-14 to 2011-09-21 when decoded as GPS time. Affected files are
silently reclassified as non-GPS and their dates are resolved via CSV or
header fallback instead.}

\item{verbose}{Logical. If \code{TRUE} (default), prints temporal extent information.}
}
\value{
When \code{per_file = TRUE}: A \code{data.frame} with columns:
\describe{
\item{filename}{Filename of the LASfile.}
\item{date}{Acquisition date (Date object) or reference year (numeric)
if \code{return_referenceyear = TRUE}.}
\item{from}{Character. One of \code{data} (files with valid GPStime), \code{csv}
(matched from CSV file), or \code{header} (from file header).}
}
When \code{per_file = FALSE}: A single-row data.frame with \code{start} and \code{end}
(Date objects or numeric years depending on \code{return_referenceyear}).
}
\description{
Extracts the temporal extent (acquisition dates) from LASfiles. Can return
dates per file or the combined date range of all files.
}
\details{
For LAS 1.3+ files with GPS time encoding, the function extracts the date from
the first point. For older files without GPS time, if \code{from_csv} is provided,
the function assigns the closest acquisition date prior to the processing date
from the CSV file based on tile coordinates. Otherwise, the date is extracted
from the LAS header (processing date).

When \code{return_referenceyear = TRUE}, November and December acquisitions are
shifted to the following year to standardize reference years.
}
\examples{
folder <- system.file("extdata", package = "managelidar")
las_files <- list.files(folder, full.names = TRUE, pattern = "*.laz")

# Get dates per file
las_files |> get_temporal_extent()

# Get combined date range
las_files |> get_temporal_extent(per_file = FALSE)

# Get reference years
las_files |> get_temporal_extent(return_referenceyear = TRUE)

# Using CSV for reference dates
csv_path <- system.file("extdata", "acquisition_dates.csv", package = "managelidar")
get_temporal_extent(folder, from_csv = csv_path)

}
\seealso{
\code{\link{get_spatial_extent}}, \code{\link{filter_temporal}}
}
