% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_vpcs.r
\name{merge_vpcs}
\alias{merge_vpcs}
\title{Merge multiple Virtual Point Cloud (VPC) files}
\usage{
merge_vpcs(vpc_files, out_file = NULL, overwrite = FALSE)
}
\arguments{
\item{vpc_files}{Character. Paths to one or more \code{.vpc} files.}

\item{out_file}{Optional. Path to write the merged \code{.vpc} file. If \code{NULL},
a temporary file is created.}

\item{overwrite}{Logical. If \code{TRUE}, overwrite the output file if it exists.}
}
\value{
A list representing the merged VPC (STAC FeatureCollection).
Invisibly returns the merged VPC. If \code{out_file} is provided, the file
is written as a valid \code{.vpc} JSON.
}
\description{
\code{merge_vpcs()} reads one or more \code{.vpc} files, merges their features,
and removes duplicate tiles (based on storage location). Optionally, the merged
VPC can be written to a file.
}
\examples{
# Merge two VPC files
merged <- merge_vpcs(c("vpc1.vpc", "vpc2.vpc"))

}
