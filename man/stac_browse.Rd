% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_browse.R
\name{stac_browse}
\alias{stac_browse}
\title{Browse a STAC catalog}
\usage{
stac_browse(catalog, tool = "stac-browser")
}
\arguments{
\item{catalog}{Path to \code{catalog.json} (the root of the STAC tree).}

\item{tool}{Can be either \code{"stac-browser"} (default) or \code{"stac-map"}.}
}
\value{
Nothing (called for its side effect of opening a browser).
}
\description{
Opens a STAC catalog (created with \code{\link[=stac_create_catalog]{stac_create_catalog()}}) in your
browser so you can explore its collections and items visually.
Uses either \href{https://github.com/developmentseed/stac-map}{STAC Map} or \href{https://github.com/radiantearth/stac-browser}{STAC Browser}.
}
\examples{
cat <- tempfile("stac-managelidar-") |>
  stac_create_catalog(id = "lidar_ni") 
col <- cat |>
  stac_add_collection(id = "lidar_ni", title = "Lidar Solling")
cat |> stac_browse()

}
