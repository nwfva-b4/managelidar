% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_helpers.R
\name{set_link}
\alias{set_link}
\title{Replace the link with a given \code{rel}, or add it if not present}
\usage{
set_link(links, new_link)
}
\arguments{
\item{links}{List of link objects}

\item{new_link}{The link object to set (its \code{rel} determines what gets
replaced)}
}
\value{
Updated links list
}
\description{
For relation types that should only appear once per object (\code{root},
\code{parent}, \code{self}, \code{icon}). Not suitable for \code{child}/\code{item} links, which
are multi-valued - see \code{\link[=add_child_link]{add_child_link()}} and \code{\link[=rebuild_item_links]{rebuild_item_links()}}
for those.
}
\details{
Replaces in place (at the existing link's position) rather than
removing and re-appending, so a call that doesn't actually change
anything doesn't reorder the links list either - that reordering would
otherwise make before/after equality checks (used to detect whether a
write is actually needed) report a change that isn't really there.
}
\keyword{internal}
