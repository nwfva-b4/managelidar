% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_vpc_enriched copy.R,
%   R/create_vpc_enriched.R
\name{create_vpc_enriched}
\alias{create_vpc_enriched}
\title{Create enriched Virtual Point Cloud with outlines and summary metadata}
\usage{
create_vpc_enriched(
  path,
  outlines = NULL,
  metadata = NULL,
  out_file = NULL,
  verbose = TRUE
)

create_vpc_enriched(
  path,
  outlines = NULL,
  metadata = NULL,
  out_file = NULL,
  verbose = TRUE
)
}
\arguments{
\item{path}{Character. Path to LAS/LAZ/COPC file(s), directory, VPC file(s),
or VPC object.}

\item{outlines}{Character or logical. Directory containing outline GeoJSON files.
If NULL (default), looks for an 'outlines' directory adjacent to the input files.
If FALSE, geometry will not be updated from outlines.}

\item{metadata}{Character or logical. Directory containing summary JSON files.
If NULL (default), looks for a 'metadata' directory adjacent to the input files.
If FALSE, metadata properties will not be added.}

\item{out_file}{Character. Output VPC file path. If NULL (default), returns
the enriched VPC as an R object.}

\item{verbose}{Logical. Print progress messages (default: TRUE).}

\item{outline_dir}{Character. Directory containing outline GeoJSON files. If NULL,
outlines are computed on-the-fly (requires reading full point clouds).}

\item{metadata_dir}{Character. Directory containing summary JSON files. If NULL,
summaries are computed on-the-fly (requires reading full point clouds).}
}
\value{
If \code{out_file} is NULL, returns the enriched VPC as a list.
If \code{out_file} is provided, saves to file and returns the file path.

If \code{out_file} is NULL, returns the enriched VPC as a list.
If \code{out_file} is provided, saves to file and returns the file path.
}
\description{
Creates an enriched VPC from LAS/LAZ/COPC files with detailed outline geometries
and summary statistics.

Creates an enriched VPC from LAS/LAZ/COPC files with detailed outline geometries
and summary statistics.
}
\details{
This function enriches VPC features with:
\itemize{
\item Detailed outline geometries (WGS84 polygons instead of bounding boxes)
\item Point and pulse density statistics
\item Per-dimension statistics (Z, Intensity, GpsTime)
\item Classification and return number distributions
}

\strong{Performance Note:} When \code{outline_dir} or \code{metadata_dir} are NULL, the function
must read the complete point cloud for each file to compute outlines and summaries.
This is \strong{very slow} for large datasets.

The enriched VPC follows the STAC pointcloud extension specification with
additional custom properties for point and pulse density.

This function is typically used after \code{\link{raw_to_processed}} to create
a collection-level VPC with enhanced metadata.

This function enriches VPC features with:
\itemize{
\item Detailed outline geometries (WGS84 polygons instead of bounding boxes)
\item Point and pulse density statistics
\item Per-dimension statistics (Z, Intensity, GpsTime)
\item Classification and return number distributions
}

\strong{Typical workflow:}
\enumerate{
\item Run \code{\link{raw_to_processed}} to create processed point clouds with
outlines and metadata in standardized directories
\item Run \code{create_vpc_enriched} to create a collection VPC with enhanced
metadata from those directories
}

\strong{Auto-detection:} When \code{outlines} or \code{metadata} are NULL, the function
looks for directories named 'outlines' and 'metadata' in the parent directory
of the input files (as created by \code{\link{raw_to_processed}}).

\strong{Selective enrichment:} Set \code{outlines = FALSE} to skip geometry enrichment,
or \code{metadata = FALSE} to skip metadata enrichment.

The enriched VPC follows the STAC pointcloud extension specification with
additional custom properties for point and pulse density.
}
\examples{
\dontrun{
# Use pre-computed outlines and metadata
vpc <- create_vpc_enriched(
  path = "output/pointcloud",
  outline_dir = "output/outlines",
  metadata_dir = "output/metadata",
  out_file = "collection.vpc"
)

# Compute on-the-fly (reads full point clouds)
vpc <- create_vpc_enriched(
  path = "data/file.laz",
  outline_dir = NULL,
  metadata_dir = NULL
)
}
# Typical workflow after raw_to_processed
folder <- system.file("extdata", package = "managelidar")
vpc_enriched <- folder |>
  raw_to_processed() |>
  create_vpc_enriched()
}
