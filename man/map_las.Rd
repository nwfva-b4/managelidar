% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_las.R
\name{map_las}
\alias{map_las}
\title{Map a function over LAS/LAZ/COPC files}
\usage{
map_las(files, FUN, workers = NULL)
}
\arguments{
\item{files}{Character vector of LAS/LAZ/COPC file paths.}

\item{FUN}{Function to apply to each file.}

\item{workers}{Integer or \code{NULL}. Number of parallel workers.
If \code{NULL} (default), workers are set to half of available logical cores
when 20 or more files are detected, and sequential processing is used
otherwise. Set to \code{1} to force sequential processing regardless of file
count. Set to a positive integer to force that number of workers.}
}
\value{
A list with one element per file. Failed files return a list with
\code{output = NULL} and a \code{log} entry with \code{status = "failed"}.
}
\description{
Internal helper to apply a function to multiple LASfiles, optionally using
parallel processing via mirai. Errors in individual files are caught and
returned as structured failure entries rather than propagating.
}
\keyword{internal}
