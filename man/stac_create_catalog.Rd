% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_functions.R
\name{stac_create_catalog}
\alias{stac_create_catalog}
\title{Create (or update) a STAC catalog}
\usage{
stac_create_catalog(
  path,
  id,
  title = id,
  description = "STAC catalog",
  icon = NULL,
  copy = NULL
)
}
\arguments{
\item{path}{Directory in which to create the catalog. \code{catalog.json} is
written inside this directory.}

\item{id}{Catalog ID. Ignored (with a warning) if a catalog already
exists at \code{path} with a different ID, since IDs aren't renamed here.}

\item{title}{Catalog title. Defaults to \code{id} for a new catalog; left
unchanged on update unless explicitly passed.}

\item{description}{Catalog description. Defaults to \code{"STAC catalog"}
for a new catalog; left unchanged on update unless explicitly passed.}

\item{icon}{A URL, or a path to a local image file, to set as the
catalog's icon link (shown in headers/listings by STAC Browser and
similar clients). See \code{\link[=stac_set_icon]{stac_set_icon()}} for details.}

\item{copy}{Whether to copy \code{icon} into the catalog tree rather than
reference it in place. See \code{\link[=stac_set_icon]{stac_set_icon()}} for the default
behavior.}
}
\value{
Path to \code{catalog.json} (invisibly), for piping into
\code{\link[=stac_add_collection]{stac_add_collection()}}.
}
\description{
Creates a new root STAC catalog at the given path. This is the entry
point of a STAC tree; collections are added underneath it with
\code{\link[=stac_add_collection]{stac_add_collection()}}.
}
\details{
If a catalog already exists at \code{path}, it is updated in place instead
of being recreated: any argument you explicitly pass (\code{title},
\code{description}, \code{icon}) is applied; anything you don't pass is left
untouched, so re-running this with just a new \code{icon} won't reset the
title back to its default. Existing links (e.g. \code{child} links to
collections added since) are preserved.
}
\examples{
tempfile("stac-managelidar-") |>
  stac_create_catalog(id = "lidar_ni", title = "Sample Catalog")

}
