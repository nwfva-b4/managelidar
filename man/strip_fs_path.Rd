% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_helpers.R
\name{strip_fs_path}
\alias{strip_fs_path}
\title{Recursively strip the \code{fs_path} S3 class from any values in a STAC
object}
\usage{
strip_fs_path(x)
}
\arguments{
\item{x}{Any R object (typically a STAC list structure)}
}
\value{
The same structure with \code{fs_path} classing removed
}
\description{
Hrefs built via \code{fs::path()}/\code{fs::path_rel()} carry an \code{fs_path} class
on top of the underlying character value. The same href read back from
JSON after a round-trip (via \code{\link[=read_stac]{read_stac()}}) has no such class - so a
plain \code{identical()} between a freshly-rebuilt object (fs_path hrefs)
and one just read from disk (plain character hrefs) reports "changed"
even when every string value actually matches. Every change-detection
comparison in this package runs both sides through this first, the
same way \code{\link[=extents_equal]{extents_equal()}} normalizes away the matrix-vs-list
representation difference for extents.
}
\keyword{internal}
