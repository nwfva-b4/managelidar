% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_first.R
\name{get_first}
\alias{get_first}
\title{Get first acquisition from multi-temporal tiles}
\usage{
get_first(
  path,
  entire_tiles = TRUE,
  tolerance = 1,
  full.names = FALSE,
  multitemporal_only = FALSE,
  out_file = NULL
)
}
\arguments{
\item{path}{Character vector of input paths, a VPC file path, or a VPC object
already loaded in R. Can be a mix of LAS/LAZ/COPC files and \code{.vpc} files.}

\item{entire_tiles}{Logical. If \code{TRUE} (default), only considers tiles where
the entire tile area has multi-temporal coverage. If \code{FALSE}, includes tiles
with partial multi-temporal coverage.}

\item{tolerance}{Numeric. Tolerance in coordinate units for snapping extents to grid
(default: 1, submeter inaccuaries are ignored). If > 0, coordinates within this distance of a grid line will be
snapped before processing. Set to 0 to disable snapping.}

\item{full.names}{Logical. Whether to return full file paths (default: FALSE)}

\item{multitemporal_only}{Logical. If \code{TRUE}, only returns tiles with multiple
acquisitions. If \code{FALSE} (default), includes all tiles.}

\item{out_file}{Optional. Path where a filtered VPC file should be saved. If
\code{NULL} (default), returns a data frame. If provided, writes a VPC file
containing only the first acquisitions and returns the file path.}
}
\value{
If \code{out_file} is \code{NULL}, returns a data frame with columns:
\describe{
\item{filename}{Character. Path to the LAS/LAZ file}
\item{date}{Date. Acquisition date of the file}
}
If \code{out_file} is provided, returns the path to the saved VPC file.
}
\description{
Identifies tiles with multiple acquisitions and returns only the first (earliest)
acquisition for each tile. Can return results as a data frame or write a filtered
VPC file.
}
\details{
The function performs the following steps:
\enumerate{
\item Resolves input paths to a VPC object
\item Checks for multi-temporal coverage using \code{\link{check_multitemporal}}
\item Groups tiles by location and selects the earliest acquisition for each
\item Returns either a summary data frame or writes a filtered VPC file
}
}
\examples{
f <- system.file("extdata", package = "managelidar")
get_first(f)

}
\seealso{
\code{\link{get_latest}}, \code{\link{check_multitemporal}}, \code{\link{resolve_vpc}}
}
