% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_functions.R
\name{stac_add_items}
\alias{stac_add_items}
\title{Add LASfile items to an existing STAC collection}
\usage{
stac_add_items(collection, path, overwrite_items = FALSE, footprints = TRUE)
}
\arguments{
\item{collection}{Path to an existing \code{collection.json}, typically the
output of \code{\link[=stac_add_collection]{stac_add_collection()}}.}

\item{path}{Character vector of input paths, or a list containing VPC
objects. Can be a mix of file paths (strings) and VPC objects (lists
with \code{type = "FeatureCollection"}).}

\item{overwrite_items}{Logical. If \code{TRUE}, overwrite existing item files.
Default is \code{FALSE} (skip existing items).}

\item{footprints}{Logical. If \code{TRUE} (default), also maintain a
footprints GeoPackage asset built from the combined VPC via
\code{\link[=write_gpkg]{write_gpkg()}} - one feature per item with queryable metadata
attributes, registered as \code{collection_obj$assets$footprints}. The
combined VPC asset itself (\code{collection_obj$assets$vpc}) is always
maintained regardless of this argument.}
}
\value{
Path to \code{collection.json} (invisibly), for piping into further
\code{\link[=stac_add_items]{stac_add_items()}} calls.
}
\description{
Converts LASfiles (via VPC) to STAC items and writes them into an
existing collection, updating the collection's extent and summaries.
}
\details{
If the collection is currently empty, its placeholder extent is
\emph{replaced} with the extent computed from this batch of items. If the
collection already contains items, the new extent is \emph{merged} with the
existing one instead.
}
\examples{
cat <- tempfile("stac-managelidar-") |>
  stac_create_catalog(id = "lidar_ni") 
col |>
  stac_add_collection(id = "lidar_ni_2023", title = "Lidar Solling 2023") |>
  stac_add_collection(id = "2023_q3", title = "Q3 2023 Data")

folder <- system.file("extdata", package = "managelidar")
las_files <- list.files(folder, full.names = T, pattern = "*.laz")
items <- col |>
  stac_add_items(path = las_files)

}
