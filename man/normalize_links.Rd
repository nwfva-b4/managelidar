% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_helpers.R
\name{normalize_links}
\alias{normalize_links}
\title{Normalize a links structure into a plain list of link objects}
\usage{
normalize_links(links)
}
\arguments{
\item{links}{Links as returned by \code{\link[=read_stac]{read_stac()}} (data frame) or already
a list of link objects}
}
\value{
List of link objects (named lists), never containing NA/NULL
field values
}
\description{
yyjsonr represents an array of link objects as a data frame when read
from disk, filling any field missing on a given link (e.g. \code{title}) with
an explicit \code{NULL} (per \code{df_missing_list_elem = "null"}). Serializing
that data frame back out would emit those as literal \code{"title": null},
which is invalid for a STAC link (\code{title} must be a string or absent).
This function converts either representation into a uniform list of
named lists, dropping NA/NULL optional fields entirely so a link is
either a proper string or not present in the output at all.
}
\keyword{internal}
