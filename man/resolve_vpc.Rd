% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve_vpc.r
\name{resolve_vpc}
\alias{resolve_vpc}
\title{Resolve input paths to a single deduplicated VPC}
\usage{
resolve_vpc(paths, out_file = NULL)
}
\arguments{
\item{paths}{Character vector of input paths, or a list containing VPC objects.
Can be a mix of file paths (strings) and VPC objects (lists with type="FeatureCollection").}

\item{out_file}{Optional. Path where the VPC should be saved. If NULL (default),
returns the VPC as an R object. If provided, saves to file and returns the file path.}
}
\value{
If \code{out_file} is NULL, returns a list containing the VPC structure.
If \code{out_file} is provided, returns the path to the saved \code{.vpc} file.
}
\description{
Takes a mix of LAS/LAZ/COPC files, \code{.vpc} files, and VPC objects already loaded in R,
merges them if needed, deduplicates tiles (based on storage location), and returns a
VPC object or file path.
}
\keyword{internal}
