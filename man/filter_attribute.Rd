% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_attribute.R
\name{filter_attribute}
\alias{filter_attribute}
\title{Filter VPC features by attribute values}
\usage{
filter_attribute(path, ..., verbose = TRUE)
}
\arguments{
\item{path}{Character vector of input paths, a VPC file path, or a VPC object
already loaded in R. Can be a mix of LAS/LAZ/COPC files and \code{.vpc} files.}

\item{...}{Logical expressions using property names. Multiple conditions are
combined with AND. Use backticks for properties with special characters
(e.g., \code{`pc:count` > 10000}).}

\item{verbose}{Logical. If TRUE (default), prints information about filtering results.}
}
\value{
A VPC object (list) containing only features matching the criteria.
Returns NULL invisibly if no features match the filter.
}
\description{
Filters VPC features based on property values using dplyr-style expressions.
}
\details{
This function filters VPC features based on their properties using familiar
dplyr-style syntax. You can use standard comparison operators and combine
multiple conditions.

\strong{Available properties in standard VPCs:}

Standard VPC files (as created by lasR) contain these properties:
\itemize{
\item \code{id} - Feature identifier (exposed for convenience, not in properties)
\item \code{datetime} - Acquisition date/time (ISO 8601 format)
\item \code{`pc:count`} - Total point count
\item \code{`pc:type`} - Point cloud type (typically "lidar")
\item \code{`proj:bbox`} - Projected bounding box (xmin, ymin, xmax, ymax)
\item \code{`proj:epsg`} - EPSG code of the CRS
\item \code{`proj:wkt2`} - WKT2 CRS definition
}

Enriched VPCs (created with \code{\link{create_vpc_enriched}}) also contain:
\itemize{
\item \code{pointdensity} - Points per square meter
\item \code{pulsedensity} - Pulses per square meter
\item \code{`pc:statistics`} - Statistical summaries (nested structure)
}

\strong{Note:} For spatial and temporal filtering, use the dedicated functions
\code{\link{filter_spatial}} and \code{\link{filter_temporal}} which handle
coordinate transformations and date parsing automatically.

\strong{Supported operators:}
\itemize{
\item Comparison: \code{>}, \code{>=}, \code{<}, \code{<=}, \code{==}, \code{!=}
\item Set membership: \code{\%in\%}
\item Logical: \code{&} (and), \code{|} (or), \code{!} (not)
}

\strong{Using property names:}
\itemize{
\item Properties without special characters can be used directly: \code{datetime}, \code{id}
\item Properties with \code{:} or \code{-} require backticks: \code{`pc:count`}, \code{`proj:epsg`}
\item String values need quotes: \code{`pc:type` == "lidar"}
}

Features missing the specified properties are excluded from results.
}
\examples{
folder <- system.file("extdata", package = "managelidar")

# Filter by point count (use backticks for properties with special chars)
folder |>
  filter_attribute(`pc:count` > 5000)

# Filter by type
folder |>
  filter_attribute(`pc:type` == "lidar")

# Multiple conditions (AND)
folder |>
  filter_attribute(`pc:count` > 5000, `pc:type` == "lidar")

# Using OR
folder |>
  filter_attribute(`pc:count` > 10000 | `pc:type` == "lidar")

# Filter by feature ID (exposed for convenience)
folder |>
  filter_attribute(id == "3dm_32_547_5724_1_ni_20240327")

# Filter by multiple IDs
folder |>
  filter_attribute(id \%in\% c(
    "3dm_32_547_5724_1_ni_20240327",
    "3dm_32_548_5724_1_ni_20240327"
  ))

# Filter enriched VPC by density
folder |>
  create_vpc_enriched() |>
  filter_attribute(pointdensity >= 10)

# Chain with dedicated filter functions
folder |>
  filter_temporal("2024-03") |>
  filter_attribute(`pc:count` > 5000) |>
  filter_spatial(c(547900, 5724900, 548100, 5725100))

# Note: For spatial/temporal filtering, prefer dedicated functions:
folder |>
  filter_temporal("2024-03-27") # Better than filter_attribute(datetime == ...)

folder |>
  filter_spatial(bbox) # Better than filter_attribute with proj:bbox
}
\seealso{
\code{\link{filter_temporal}}, \code{\link{filter_spatial}},
\code{\link{filter_multitemporal}}
}
