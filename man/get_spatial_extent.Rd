% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_spatial_extent.R
\name{get_spatial_extent}
\alias{get_spatial_extent}
\title{Get the spatial extent of LASfiles}
\usage{
get_spatial_extent(
  path,
  per_file = TRUE,
  full.names = FALSE,
  as_sf = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{path}{Character. Path to a LAS/LAZ/COPC file, a directory, or a Virtual Point Cloud (.vpc) referencing these files.}

\item{per_file}{Logical. If \code{TRUE} (default), returns extent per file. If \code{FALSE}, returns combined extent of all files.}

\item{full.names}{Logical. If \code{TRUE}, filenames in the output are full paths; otherwise base filenames (default).
Only used when \code{per_file = TRUE}.}

\item{as_sf}{Logical. If \code{TRUE}, returns an \code{sf} object with geometry. If \code{FALSE} (default), returns a data.frame.}

\item{verbose}{Logical. If \code{TRUE} (default), prints extent information.}
}
\value{
When \code{per_file = TRUE}: A \code{data.frame} or \code{sf} object with columns:
\describe{
\item{filename}{Filename of the LASfile.}
\item{xmin}{Minimum X coordinate.}
\item{xmax}{Maximum X coordinate.}
\item{ymin}{Minimum Y coordinate.}
\item{ymax}{Maximum Y coordinate.}
\item{geometry}{(optional) Polygon geometry if \code{as_sf = TRUE}.}
}
When \code{per_file = FALSE}: A single-row data.frame or sf object with the combined extent.
}
\description{
\code{get_spatial_extent()} extracts the spatial extent (xmin, xmax, ymin, ymax) from LASfiles.
Can return extent per file or the combined extent of all files.
}
\examples{
folder <- system.file("extdata", package = "managelidar")
las_files <- list.files(folder, full.names = T, pattern = "*20240327.laz")
las_files |> get_spatial_extent()

}
\seealso{
\code{\link{get_temporal_extent}}, \code{\link{filter_spatial}}
}
