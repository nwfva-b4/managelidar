% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_helpers.R
\name{extent_is_placeholder}
\alias{extent_is_placeholder}
\title{Check whether a collection's extent is still the empty placeholder
written by stac_add_collection() (as opposed to a real extent derived
from items). Only the spatial bbox is checked, since a zero bbox is the
only placeholder signal available (STAC does not allow \code{null} bbox
values, unlike temporal intervals).}
\usage{
extent_is_placeholder(extent)
}
\arguments{
\item{extent}{Extent list as read from disk (bbox as matrix)}
}
\value{
Logical
}
\description{
Check whether a collection's extent is still the empty placeholder
written by stac_add_collection() (as opposed to a real extent derived
from items). Only the spatial bbox is checked, since a zero bbox is the
only placeholder signal available (STAC does not allow \code{null} bbox
values, unlike temporal intervals).
}
\keyword{internal}
