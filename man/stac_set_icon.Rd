% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_functions.R
\name{stac_set_icon}
\alias{stac_set_icon}
\title{Set an icon on a catalog or collection}
\usage{
stac_set_icon(stac_object, source, copy = NULL)
}
\arguments{
\item{stac_object}{Path to a \code{catalog.json} or \code{collection.json}.}

\item{source}{A URL, or a path to a local image file (png, jpg/jpeg,
webp, gif, or tif/tiff).}

\item{copy}{Whether to copy the image into the catalog tree rather than
reference it in place. Defaults to \code{FALSE} for URLs (already
web-accessible) and \code{TRUE} for local files (otherwise unreachable from
a browser via \code{\link[=stac_browse]{stac_browse()}}).}
}
\value{
Path to \code{stac_object} (invisibly), for piping into further
calls.
}
\description{
Adds a link with \code{rel: "icon"}. STAC Browser (and other clients) use
this - specifically a \emph{link}, not an asset - to show a small icon in
the page header and in lists of Catalogs, Collections and Items. This
works on both catalogs and collections.
}
\details{
For a preview image shown on a collection's own page (not in listings),
use \code{\link[=stac_add_collection_asset]{stac_add_collection_asset()}} with \code{roles = "thumbnail"} instead -
that's an asset, not a link, which is a different STAC Browser
convention for a different purpose.
}
\examples{
cat <- tempfile("stac-managelidar-") |>
  stac_create_catalog(id = "lidar_ni")
col <- cat |>
  stac_add_collection(id = "lidar_ni", title = "Lidar Solling", keywords = c("lidar", "ALS", "Solling", "test")) |> 
  stac_set_icon("https://raw.githubusercontent.com/nwfva-b4/managelidar/refs/heads/main/man/figures/logo.png")

}
