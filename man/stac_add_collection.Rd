% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_functions.R
\name{stac_add_collection}
\alias{stac_add_collection}
\title{Add (or update) a collection on a STAC catalog or collection}
\usage{
stac_add_collection(
  parent,
  id = NULL,
  title,
  description = "STAC collection",
  license = "other",
  keywords = NULL,
  providers = NULL,
  summaries = NULL,
  assets = NULL,
  stac_extensions = NULL,
  thumbnail = NULL,
  overview = NULL,
  icon = NULL,
  copy = NULL
)
}
\arguments{
\item{parent}{Path to the parent STAC JSON file (\code{catalog.json} or
\code{collection.json}) when \code{id} is given, to create/locate a child under
it. Or, when \code{id} is omitted, the path to the collection's own
\code{collection.json} to update it directly - see Details.}

\item{id}{Collection ID. Required to create a new collection; optional
when updating one you already have the path to (see Details).}

\item{title}{Collection title. Defaults to \code{id} for a new collection;
left unchanged on update unless explicitly passed.}

\item{description}{Collection description. Defaults to
\code{"STAC collection"} for a new collection; left unchanged on update
unless explicitly passed.}

\item{license}{License string. Can be provided as \href{https://spdx.org/licenses/}{SPDX License identifier} or \href{https://spdx.github.io/spdx-spec/v2.3/SPDX-license-expressions/}{SPDX-license-expression}.
Defaults to \code{"other"} for a new collection; left unchanged on update unless explicitly passed.}

\item{keywords}{Character vector of keywords.}

\item{providers}{List of provider objects, e.g.
\code{list(list(name = "Org Name", roles = c("host", "producer"), url = "https://example.org"))}.
Valid roles are \code{"licensor"}, \code{"producer"}, \code{"processor"}, \code{"host"}.}

\item{summaries}{List of summary objects. \code{proj:epsg} and \code{pc:type} are
added automatically by \code{\link[=stac_add_items]{stac_add_items()}} once items are added.}

\item{assets}{List of asset objects. For a thumbnail/overview image,
prefer the \code{thumbnail}/\code{overview} arguments below instead of building
this by hand.}

\item{stac_extensions}{Character vector of extension URLs.}

\item{thumbnail}{A URL, or a path to a local image file, to set as the
collection's thumbnail asset. See \code{\link[=stac_add_collection_asset]{stac_add_collection_asset()}}.}

\item{overview}{A URL, or a path to a local image file, to set as the
collection's overview asset. See \code{\link[=stac_add_collection_asset]{stac_add_collection_asset()}}.}

\item{icon}{A URL, or a path to a local image file, to set as the
collection's icon link (shown in headers/listings). See
\code{\link[=stac_set_icon]{stac_set_icon()}}.}

\item{copy}{Whether to copy \code{thumbnail}/\code{overview}/\code{icon} into the
catalog tree rather than reference them in place. See
\code{\link[=stac_add_collection_asset]{stac_add_collection_asset()}}/\code{\link[=stac_set_icon]{stac_set_icon()}} for the default
behavior.}
}
\value{
Path to the collection's \code{collection.json} (invisibly), for
piping into \code{\link[=stac_add_collection]{stac_add_collection()}} (subcollection), \code{\link[=stac_add_items]{stac_add_items()}},
or a later update call.
}
\description{
Creates a new collection nested under a catalog or another collection.
A freshly created collection starts out empty; use \code{\link[=stac_add_items]{stac_add_items()}}
to populate it with items afterwards.
}
\details{
There are two ways to target an existing collection for update:
\itemize{
\item Pass \code{parent} + \code{id} as usual (the combination used to create
it). If a collection with that \code{id} already exists under \code{parent},
it's updated in place instead of recreated.
\item Omit \code{id} and pass the collection's own \code{collection.json} path
as \code{parent} instead - useful when you already have that path (e.g.
from an earlier pipe) and don't want to look up or retype its
\code{id}/location. \code{parent} in this mode means "the collection to
update", not "its parent".
}
Either way, any argument you explicitly pass (\code{title}, \code{description},
\code{license}, \code{keywords}, \code{providers}, \code{summaries}, \code{assets},
\code{stac_extensions}, \code{thumbnail}, \code{overview}, \code{icon}) is applied;
anything you don't pass is left untouched, so re-running this to just
add \code{providers} won't reset the title or wipe out items already added.
The collection's \code{extent} is never touched here - only
\code{\link[=stac_add_items]{stac_add_items()}} manages it.

Directory structure created (when creating via \code{parent} + \code{id}):
\itemize{
\item For catalog parent: \verb{\{parent_dir\}/collections/\{id\}/}
\item For collection parent: \verb{\{parent_dir\}/\{id\}/} (subcollection)
}

A newly created collection is written with a placeholder empty extent
(zero bbox, \code{NULL} temporal interval), which \code{\link[=stac_add_items]{stac_add_items()}}
replaces with the real extent the first time items are added.
}
\examples{
col <- tempfile("stac-managelidar-") |>
  stac_create_catalog(id = "lidar_ni") |>
  stac_add_collection(id = "lidar_ni_2023", title = "Lidar Solling 2023") |>
  stac_add_collection(id = "2023_q3", title = "Q3 2023 Data")

# Update later using just the path - no need to know/retype its id
col |> stac_add_collection(providers = list(
  list(name = "NW-FVA", roles = c("host", "processor"), url = "https://www.nw-fva.de/")
))

}
