% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stac_helpers.R
\name{resolve_image_asset}
\alias{resolve_image_asset}
\title{Resolve an image source (local path or URL) into an href usable in a
STAC link or asset, optionally copying it into the catalog tree first}
\usage{
resolve_image_asset(source, containing_dir, key, copy = NULL)
}
\arguments{
\item{source}{A URL (\verb{http://}/\verb{https://}), or a path to a local image
file}

\item{containing_dir}{Directory of the catalog/collection file this
image is being attached to; hrefs are made relative to this}

\item{key}{Base filename (without extension) to use if the image is
copied/downloaded}

\item{copy}{Logical, or \code{NULL} to use the default described above}
}
\value{
List with \code{href} (character) and \code{type} (character or \code{NULL})
}
\description{
Local files default to being copied into \verb{\{containing_dir\}/assets/},
since a bare local path (or even a \verb{file://} URI) can't be loaded by a
browser viewing the catalog through \code{\link[=stac_browse]{stac_browse()}}. URLs default to
being referenced in place, since they're already web-accessible; pass
\code{copy = TRUE} to download and vendor them into the catalog instead (so
the catalog keeps working even if the original URL later goes away).
}
\keyword{internal}
